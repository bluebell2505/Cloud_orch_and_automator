input {
  http {
    port => 5044
    codec => json
  }
}

filter {
  if [failure_type] {
    mutate {
      add_tag => ["ci_failure"]
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "ci-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}